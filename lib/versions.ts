/**
 * РЕЕСТР ВЕРСИЙ ГЛАВНОЙ СТРАНИЦЫ — единственный источник правды.
 *
 * ⚠️ СРАВНЕНИЕ ВЕРСИЙ ЗАКРЫТО (2026-08-25): заказчик выбрал облегчённый
 * вариант **версии 8**, он стал боевой главной. Её код переехал из этой папки
 * в `components/home/home-light.tsx` (`app/page.tsx` → `HomeLight scale="v8"`),
 * и боевая главная больше НИЧЕГО не импортирует из `components/versions/`.
 *
 * **ВСЕ записи ниже переведены в `archived`** — хаб `/versions` теперь пустой,
 * прямые ссылки `/versions/v1…v9` пока работают (сравнить, если понадобится).
 * Сам хаб публично закрыт `middleware.ts`, для себя — `/admin/versions`.
 * Новые версии здесь больше не заводим: вся папка со всем содержимым готова
 * к удалению одним заходом, план — `docs/versions-cleanup-plan.md`.
 *
 * Инструкция «как добавить версию» — `docs/VERSIONS.md`, но она историческая:
 * механизм жив только до уборки.
 *
 * Здесь только метаданные, без импорта React-компонентов: файл подключается и на
 * странице-хабе, и в самих версиях, и его должно быть безопасно импортировать
 * откуда угодно. Связка «id → компонент» живёт отдельно, в
 * `components/versions/registry.tsx`.
 */

export type VersionStatus = "ready" | "draft" | "archived";

export interface VersionMeta {
  /** Часть URL: /versions/<id>. Латиница, цифры и дефис. */
  id: string;
  /** Короткое имя для кнопки и плашки наверху страницы. */
  title: string;
  /** 1–2 предложения для заказчика: чем эта версия отличается. */
  description: string;
  /**
   * `ready` — показываем заказчику;
   * `draft` — в работе, на хабе помечена и вынесена вниз;
   * `archived` — на хабе не показывается, но ссылка продолжает работать.
   */
  status: VersionStatus;
}

export const VERSIONS: VersionMeta[] = [
  {
    id: "v1",
    title: "Версия 1 — Базовая",
    description:
      "Первоначальная вёрстка по макету, без анимаций при прокрутке. Прежний HERO-баннер с плавающими карточками.",
    status: "archived",
  },
  {
    id: "v2",
    title: "Версия 2 — Лёгкие анимации",
    description:
      "То же самое, но блоки плавно появляются при прокрутке, а кнопки и карточки мягко реагируют на наведение.",
    status: "archived",
  },
  {
    id: "v3",
    title: "Версия 3 — Новый HERO (мобильная)",
    description:
      "Мобильный первый экран на весь виджет без светлого пятна по центру, плюс редакционный список вместо карточек в блоке «Почему нас выбирают». Десктоп не менялся.",
    status: "archived",
  },
  {
    id: "v4",
    title: "Версия 4 — HERO на весь экран (десктоп + мобильная)",
    description:
      "То же, что версия 3, но первый экран на весь размер и на десктопе тоже: полноэкранный bento-коллаж из 5 фото вместо прежнего баннера с плавающими карточками.",
    status: "archived",
  },
  {
    id: "v5",
    title: "Версия 5 — анимации по всему сайту",
    description:
      "Версия 4 (полноэкранный HERO) плюс анимации на каждом блоке: появление при прокрутке, подъём кнопок и лёгкий zoom фото при наведении, кольца в футере оживают «водой» при наведении курсора.",
    status: "archived",
  },
  {
    id: "v6",
    title: "Версия 6 — Кинематографичный скролл (прежняя главная)",
    description:
      "Была боевой главной с 20.08 по 25.08.2026: полноэкранный HERO плюс кинематографичный слой анимаций — каждая секция появляется «wipe» (подъём + масштаб), заголовки открываются шторкой, карточки и списки — волной по очереди. Размеры — как в макете Figma, без облегчения.",
    status: "archived",
  },
  {
    id: "v7",
    title: "Версия 7 — Компактная",
    description:
      "Тот же макет версии 6, просто уменьшенный масштаб типографики и отступов (≈15–20%). Ничего не переставлено, шрифты и содержание те же.",
    status: "archived",
  },
  {
    id: "v8",
    title: "Версия 8 — Лёгкая (ВЫБРАНА, стала боевой главной)",
    description:
      "Заметно меньше: заголовки, абзацы и отступы между блоками уменьшены ощутимее, страница проходится быстрее. Ответ на правку «хочется лёгкости, чтобы не каждый блок занимал экран». Именно этот вариант с 25.08.2026 стоит на `/` — код переехал в `components/home/home-light.tsx`, здесь остался только просмотр «как было».",
    status: "archived",
  },
  {
    id: "v9",
    title: "Версия 9 — Воздушная",
    description:
      "Кегли как в версии 8, но с ещё бо́льшим воздухом вокруг текста и облегчёнными карточками/плашками (без теней и заливок) — самое смелое отступление от макета Figma.",
    status: "archived",
  },
];

export function getVersion(id: string): VersionMeta | undefined {
  return VERSIONS.find((v) => v.id === id);
}

/** Версии для показа на хабе: `ready` сначала, `draft` следом, `archived` скрыты. */
export function getVisibleVersions(): VersionMeta[] {
  const order: Record<VersionStatus, number> = { ready: 0, draft: 1, archived: 2 };
  return VERSIONS.filter((v) => v.status !== "archived").sort(
    (a, b) => order[a.status] - order[b.status]
  );
}
