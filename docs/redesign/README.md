# Редизайн главной страницы — рабочая инструкция

**Статус:** в работе. Начато 2026-08-11.
**Ветка:** `feature/homepage-redesign` (отведена от `docs/audit-and-fixes`). Есть
чекпоинт-коммиты — `git log` перед новой правкой, чтобы не задваивать работу.
**Порядок блоков:** решением заказчика начинаем с **Header**, затем **HERO**, далее по макету сверху вниз.

Этот файл — точка входа для любого агента, который продолжает работу над редизайном.
Читать **до** первой правки. Прочие файлы папки:

⚠️ **Сравнение версий главной закрыто 2026-08-25.** Заказчик выбрал облегчённый
вариант «версия 8», он стал боевой главной (`app/page.tsx` → `HomeLight scale="v8"`
из `components/home/`), все девять версий переведены в архив. Что именно уменьшает
размеры на главной — [`lightweight-scale-plan.md`](lightweight-scale-plan.md),
**читать до любой правки размеров**: поверх закона масштабирования там лежит слой
коэффициентов, и новый элемент, написанный «по макету», окажется крупнее соседей.
Как был устроен сам механизм версий — [`docs/VERSIONS.md`](../VERSIONS.md)
(исторический), план удаления — [`docs/versions-cleanup-plan.md`](../versions-cleanup-plan.md).
`backlog.md` (пп. Н1–Н6) — как задача версий возникла и куда делся Google Stitch.

## Быстрый старт для новой сессии

**На чём остановились (2026-08-11, вечер, новая голосовая сессия):**
блоки 0–8 (Header, HERO, ABOUT, CALENDAR, VALUES, GUESTS, FOUNDERS, отзывы, COLLAB)
свёрстаны и лежат **некоммиченными** в рабочей копии — **первым делом проверь
`git status`**, скорее всего нужно закоммитить или продолжить с того, что на диске.

В этой сессии заказчик пересмотрел решение по блоку 9 (FAQ, см. ниже) и попросил
доработать нижнюю часть блока 6 (FOUNDERS) — **оба выполнены**:
- **FAQ** — заголовок блока приведён к общему стилю главной (эйбрау+H2), аккордеоны
  не тронуты. См. блок 9 в `blocks.md`.
- **FOUNDERS** — фото Анны Турбиной загружено из Figma (было пустым), карточки
  создателей переработаны (органичные асимметричные фото-рамки по форме из Figma,
  вертикальный сдвиг, декоративный `&`). См. блок 6 в `blocks.md`.

Осталось: 10 (Footer, не начат — следующий в очереди, см. `backlog.md` п.9).
Плюс большой список отложенных задач в `backlog.md` (пп. 5–11): карусель CALENDAR,
мобильная сетка VALUES, баг мобильных карточек отзывов, слипающиеся теги COLLAB на
мобильной, footer, аудит кнопок, анимация (2 версии) — **делать по одному, не
пачками**, после того как текущие правки примет заказчик.

**Один блок сознательно НЕ редизайнился** по прямому решению заказчика —
не начинать переделку без нового запроса:
- **Блок 7 (отзывы/TESTIMONIALS)** — первая попытка пересобрать под макет сломалась
  на реальных (длинных) отзывах, заказчик попросил вернуть оригинальный механизм
  (`reviews-section.tsx`/`reviews-embla.tsx`/`review-card.tsx`) как есть. Тронуты
  только цвета карточки под токены проекта + положение стрелок — см. `blocks.md`
  блок 7 для полного разбора. Баг мобильного рендера («хреновые карточки», грязное
  пятно на крае подглядывающей карточки) найден и исправлен в той же сессии — см.
  `backlog.md` п.7. Механика по-прежнему не трогается без нового запроса.

**Все прочие блоки ждут проверки заказчика.**

### ✅ Баг с обрезкой фото на CALENDAR — исправлен 2026-08-11 (вечер)

**Причина оказалась двойной, диагностирована через `get_design_context` (см. новый
`fileKey` выше):**

1. **Контейнер фото был неправильной пропорции.** Код задавал `aspect-[6/5]` (1.2), а в
   макете (нода `5:166`/`5:175`/`5:184`) контейнер фото — `334.66 × 250.98`, то есть **4:3**
   (1.333), не 6:5.
2. **Двойной кроп в [`sanity-image.tsx`](../../components/ui/sanity-image.tsx).** В режиме
   `fill` компонент всегда запрашивал у Sanity фиксированный кроп `1200×1600` (портрет, 3:4),
   не зная реальной формы контейнера на странице. Sanity обрезала фото по этому фиксированному
   прямоугольнику (с учётом hotspot), а браузер поверх ещё раз резал через CSS `object-cover`
   под фактический контейнер — второй кроп hotspot уже не учитывал. Проверено GROQ-запросом:
   реальные `cardImage` у всех туров — портретные (aspect 0.56–0.8), поэтому двойная обрезка
   резала по-разному в зависимости от того, куда попадал hotspot после первого кропа.

**Исправление:** `SanityImage` получил опциональный проп `aspectRatio` — при его передаче
компонент запрашивает у Sanity кроп той же пропорции, что и реальный контейнер (без пропа
поведение не изменилось, остальные вызовы `SanityImage fill` не затронуты). Карточка тура
теперь передаёт `aspectRatio={4/3}` и использует `aspect-[4/3]` вместо `aspect-[6/5]`.
Проверено скриншотами (Playwright) на 1920px и 390px — обрезка адекватная на всех карточках.

### Что уже сделано и проверено

- **HERO**: баг с кнопками на 1024–1919px исправлен (`vw` вместо `px` в отступах).
  Мобильная версия — 4 итерации, финальная: scrim-подложка под текстом (см.
  раздел про блок 1 в `blocks.md` — там же урок про проверку пикселей, не «на глаз»),
  фото-кластеры лево/право как на десктопе, кнопки крупнее и горизонтально.
  Фото коллажа заменены на настоящие Figma-ассеты (были случайным стоком — огрех
  более ранней сессии), старые ассеты удалены из Sanity.
- **ABOUT**: свёрстан, контент (текст + 2 фото) загружен агентом напрямую в Sanity
  из Figma через `sanity exec --with-user-token` (см. ниже — этот способ работает
  и для новой сессии, авторизация локальная, не привязана к сессии агента).
- **CALENDAR**: свёрстан по скриншоту `screens/03-calendar.png`, затем 2026-08-11 вечером
  сверен с реальным `get_design_context` (см. баг выше) и приведён в точное соответствие:
  радиус карточки `26px` (было `16px`), пропорция фото `4:3` (было `6:5`), цвет текста на
  карточке — токен `background` (`#eeeae4`, в макете `#f0ede6` — почти совпадает, п.4 правил),
  добавлены два новых токена `--color-subtle` (`#9a9a8a`) и `--color-subtle-border` (`#c8c0b3`)
  для эйбрау блока и неактивной вкладки года — этих оттенков не было в палитре, п.4 правил.
  Структура текста карточки теперь трёхуровневая, как в макете: короткая подпись-оверлей
  на фото (`overlayName`, показывается только если заполнена в Studio) → название тура →
  даты. Старые компоненты `tour-card.tsx` / `tour-card-wrapper.tsx` / `tours-embla.tsx`
  удалены, секция «Наши туры» заменена.
  **Год у всех 5 туров по-прежнему пуст в Sanity** — переключатель 2026/2027 не показывается,
  секция рендерит все туры одним рядом (код на это рассчитан, ничего не сломано,
  просто ждёт данных). Проставить год может только заказчик (это реальные даты
  бронирования, агент не угадывает).
- **VALUES**: свёрстан и наполнен, `id="values"` — якорь в шапке рабочий. Фон — **два
  разных фото** по половинам секции (45% непрозрачности), не одно растянутое и не
  «фото + заливка» — обе версии были ошибочными по ходу правки, детали и как нашли
  второе фото (протекло из необрезанного бейджа карточки) — см. `blocks.md`, блок 4.
  Добавлено поле схемы `homePage.values.backgroundImageRight`, Studio передеплоена.
  6 карточек с круглыми номерами, контент загружен агентом из Figma напрямую в Sanity.
- **GUESTS**: свёрстан и наполнен. Коллаж из 3 фото с органичными «лепестковыми»
  скруглениями (проценты от сторон, не фиксированные px — форма держится на любой
  ширине). Найден и починен системный баг — `vw`-шрифт внутри `Container` (капается
  на `max-w-screen-xl`=1280px) рос без предела на широких экранах и рвал раскладку;
  исправлено через `clamp()`, заодно тем же способом поправлены CALENDAR и VALUES.
- **FOUNDERS**: свёрстан и наполнен, **дважды** — сначала только общий блок (`57:282`:
  фото + интро + кнопка), затем по просьбе заказчика добавлены персональные карточки
  создателей (имя/роль/фото, по образцу старого макета `5:230`) — два отдельных поля
  схемы `founderOne`/`founderTwo`, не массив. Фото Анны Турбины ещё не загружено
  (заполнено только одно поле по просьбе заказчика), описания у обеих персон пустые
  (в макете были нераскрытые плейсхолдеры). См. `blocks.md` блок 6.
- **Отзывы/TESTIMONIALS** — см. предупреждение выше, не редизайнились по решению
  заказчика, только точечный рестайлинг цветов/позиции стрелок. См. `blocks.md` блок 7.
- **COLLAB**: свёрстан и наполнен. Нашлась и решена коллизия полей с уже существующим
  документом `customTour` (там уже был настоящий контент страницы `/custom-tour` —
  чуть не затёрли его тизером с главной, см. `blocks.md` блок 8, урок общий для всех
  будущих блоков, что переиспользуют существующие типы документов).
  `vw`+`Container`-ограничение учтено сразу, без отдельного захода
  (см. `backlog.md` п.0 — там же итоговый статус, все 5 затронутых блоков починены).

### Технические заметки для новой сессии

- **Запись в Sanity без диалога с заказчиком** — рабочий приём этой сессии:
  `cd sanity && ASSET_DIR=<путь> npx sanity exec <script>.cjs --with-user-token`.
  CLI уже авторизован локально (администратор проекта), скрипт получает клиент
  через `require('sanity/cli').getCliClient()` — **обязательно `.cjs`, не `.mjs`**
  (ESM-импорт `sanity/cli` в `sanity exec` ломается с "does not provide an export
  named 'getCliClient'", в CJS всё нормально). Скрипт держать временно и удалять
  после — не коммитить в `sanity/`.
- **Скриншоты** — Browser pane в этой машине не рендерит кадр
  (`computer{screenshot}` падает с "pane is not displayed"). Рабочая замена —
  отдельный Playwright в scratchpad-папке, см. память `reference-playwright-screenshot-tool`.
  Она session-scoped, если папки `shots-tool/` нет — пересоздать (быстро,
  ~10 секунд, бинарники Chromium кэшируются в `%LOCALAPPDATA%\ms-playwright`
  между сессиями). **Для подложек в цвет фона — проверять пикселями
  (`pngjs`), не на глаз**: сжатая превьюшка в чате визуально смешивает
  scrim и фото, даже когда размер эллипса задан в разы больше нужного.
- **Дев-сервер завис один раз** в этой сессии на "Compiling / ..." после очень
  долгой серии горячих перезагрузок — помогло: `preview_stop` → `rm -rf .next/dev`
  → `preview_start` заново (первая холодная компиляция после этого заняла
  ~100 секунд, это нормально, не зависание — ждать, не убивать повторно).

**Sanity Studio задеплоена и наполнена:** `https://anna-turbina-tours.sanity.studio/`
(последний деплой — 2026-08-11, поздний вечер, после добавления `founderOne`/
`founderTwo`). Документ `homePage`: заполнены `hero`, `about`, `calendar`, `values`,
`guests`, `founders` (включая обе персональные карточки, кроме фото Анны). Документ
`customTour`: заполнены `title`/`description`/`mainImage` (уже были, реальный контент
страницы `/custom-tour`) **и** новые `homeHeading`/`homeHeadingAccent`/
`homeDescription`/`images`/`tags`/`eyebrow` (для блока COLLAB на главной). Не заполнено
намеренно/за заказчиком: год у туров (CALENDAR), ссылки Instagram и фото Анны
(FOUNDERS), описания создателей (FOUNDERS). `testimonials`/`faq` в схеме есть, но не
используются — эти блоки не редизайнились (см. выше).

**Локальный сервер:** поднимать через Browser pane `preview_start({name:"next-dev"})`
(не через `Bash`/`PowerShell` напрямую для превью — см. правила инструментов).
Смотреть главную: `http://localhost:3000/admin/`. С телефона в той же Wi-Fi:
`http://192.168.0.39:3000/admin/` (IP может измениться — `ipconfig`).

**Правило, добавленное 2026-08-11 после реального сбоя процесса** (заказчик потратил
~50 минут, не понимая, почему на сайте пусто) — см. п.6 в «Жёсткие правила» ниже:
**каждый отчёт по блоку, зависящему от Sanity, обязан начинаться с явного чек-листа
«что заполнить в Studio», и Studio должна быть задеплоена сразу после правки схемы,
не когда попросят.**

---

| Файл | О чём |
|---|---|
| [`blocks.md`](blocks.md) | Реестр блоков: Figma-нода → компонент → источник данных → статус → комментарии заказчика |
| [`sanity-plan.md`](sanity-plan.md) | Карта контента Sanity: типы, что откуда берётся, что удалено, что заполнить руками |
| [`legacy.md`](legacy.md) | Компоненты старого дизайна: что сохраняем и как вернуть |
| [`backlog.md`](backlog.md) | Отложенные задачи и открытые вопросы |
| [`client-feedback-2026-08.md`](client-feedback-2026-08.md) | **Правки заказчика от августа 2026 — разбор, что подтвердилось, план и открытые вопросы** |
| [`screens/`](screens/) | Скриншоты всех блоков из макета |

---

## Что мы делаем

Заказчик прислал готовый дизайн главной страницы в Figma. Задача — **заменить текущую
главную на этот дизайн**, разбирая его по блокам.

**Источник дизайна:**
`https://www.figma.com/design/6XZ3iT1j6b3HpGmdVEGhH5/ONA-Main-Local?node-id=0-1`

- `fileKey` = `6XZ3iT1j6b3HpGmdVEGhH5`
- Лист `Page 1` = нода `0:1`
- Корневой фрейм страницы = `5:2` (1920 × 9551), структура и node-id узлов совпадают с тем,
  что было в предыдущих сессиях (это тот же макет, просто актуальная копия файла).

⚠️ **2026-08-11, вечер — старый `fileKey` (`1FMa8mpXz5izCNZ9kAwpnx`) больше не использовать.**
Он упирается в лимит Starter-тарифа независимо от аккаунта (`whoami` показывает Pro/Dev на
команде агента, но сам файл принадлежит другой Figma-команде с Starter) — `get_metadata` и
`get_design_context` стабильно отдают ошибку лимита. Заказчик прислал ссылку на актуальную
копию файла в своей команде (тариф Pro/Dev, лимитов нет) — `fileKey` выше уже обновлён на неё.
Если в новой сессии Figma MCP снова упрётся в лимит — проверить, не используется ли по
инерции старый `fileKey` откуда-то из памяти агента.

Доступ к Figma — через **remote MCP-сервер** (`https://mcp.figma.com/mcp`, HTTP, OAuth).
Локальное расширение `ant.dir.ant.figma.figma` (инструменты `mcp__Figma__*`) требует
запущенного десктопного Figma и **не используется** — брать remote-инструменты.

Полезные вызовы:
```
get_metadata(fileKey, nodeId)      — структура поддерева
get_screenshot(fileKey, nodeId)    — PNG (отдаёт короткоживущий URL, качать curl'ом)
get_design_context(fileKey, nodeId)— код + вычисленные значения цветов/размеров + ассеты
```

⚠️ `get_variable_defs` на этом файле возвращает `{}` — **переменных Figma в файле нет**.
Автоматического маппинга «переменная → токен» не будет, значения снимать через
`get_design_context` и вручную сопоставлять с токенами проекта.

⚠️ Файл — импорт с существующего сайта (плагин типа html.to.design): имена слоёв вида
`Slot → Summary`, `Paragraph+Background`, картинки `IMG_0744 1`, автолэйаутов почти нет,
координаты абсолютные. **Макет — референс по внешнему виду, а не по структуре разметки.**
Верстаем по-своему: адаптивно, на Tailwind, с нормальной семантикой.

---

## Жёсткие правила

### 1. Шрифты не меняем

Из дизайна берём **только композицию, цвета, размеры, отступы**. Шрифты остаются проектные,
из [`lib/fonts.ts`](../../lib/fonts.ts):

| Роль | Шрифт | Как применять |
|---|---|---|
| Заголовки | Cormorant | `font-heading` (и так по умолчанию на `h1`–`h6`) |
| Основной текст | Kinetika | по умолчанию на `body` |
| Логотип | LaLuxes | `font-logo` |
| Подзаголовок логотипа | MADE TheArtist Script | `font-logo-subtitle` |

`lib/fonts.ts` — **генерируемый файл**, руками не править.

### 2. Бэкенд не переделываем

Главный принцип задачи: **дизайн — это только отображение**. Всё, что уже приходит из Sanity,
переиспользуем как есть, новые схемы заводим только там, где контента реально нет.

Уже готово и должно быть переиспользовано без изменений:

| Данные | Тип Sanity | Запрос в `lib/sanity.queries.ts` | Готовые хелперы |
|---|---|---|---|
| Туры (карточки) | `tour` | `toursQuery` | `isTourVisibleOnSite()` |
| Отзывы | `tour.reviews[]` | `toursWithReviewsQuery` | `flattenReviewsFromTours()` |
| Индивидуальный тур | `customTour` | `customTourQuery` | — |
| FAQ | `faq` | `faqQuery` | — |
| Заголовки блоков главной | `homePage` (синглтон) | `homePageQuery` | — |
| Футер и контакты | `siteSettings` (синглтон) | `siteSettingsQuery` | — |

⚠️ Тип `about` («Обо мне») **удалён** — такого раздела нет в макете. Не путать со схожим
по названию новым блоком «О проекте» (`homePage.about`) — это другая сущность.
Подробности и история решения — [`sanity-plan.md`](sanity-plan.md), [`legacy.md`](legacy.md).

Схема `homePage` **заведена сразу вся** (все 9 секций), не по одному блоку — решение
заказчика от 2026-08-11. Дальше по блокам меняется только вёрстка, в схему лезть
не нужно, кроме случаев, когда всплывёт что-то незапланированное.

Новое заводим по правилу проекта — **3 правки**: схема в `sanity/schemas/` → запрос в
`lib/sanity.queries.ts` → рендер в компоненте. Инлайновых GROQ в компонентах не делаем.

### 3. Все картинки — из Sanity

Никаких файлов из макета в `public/`. Каждое фото в новых блоках — поле в Sanity,
рендер через `<SanityImage>` из [`components/ui/sanity-image.tsx`](../../components/ui/sanity-image.tsx).
Под это заведён документ-синглтон `homePage` — см. [`sanity-plan.md`](sanity-plan.md).

### 4. Цвета — только токены

Палитра дизайна предварительно **совпадает** с существующей в
[`app/globals.css`](../../app/globals.css): фон `#EEEAE4` = `--color-background`,
оливковая кнопка ≈ `#69695C` = `--color-primary`, текст `#1E1E1C` = `--color-foreground`.
Сверять на каждом блоке через `get_design_context`. Хардкод hex — запрещён;
если в дизайне встретился цвет вне палитры — не хардкодить, а завести токен в `@theme`
и **отметить это в отчёте заказчику**.

### 5. Серверные компоненты по умолчанию

`"use client"` — только там, где нужны хуки/DOM: карусели, аккордеоны, анимации на
IntersectionObserver, переключатели. Данные грузим в `app/page.tsx` (серверный) и
прокидываем пропсами.

### 6. После КАЖДОГО блока — явный чек-лист действий в Sanity

**Это правило появилось после реального провала 2026-08-11:** блок HERO был готов
в коде, но заказчик потратил ~50 минут, пытаясь понять, почему на сайте пусто.
Причина — данные лежат в Sanity, а документ `homePage` не был создан и опубликован.
Это было сказано в отчёте, но **одной строкой в середине текста** — потерялось.

Начиная с этого момента, если блок зависит от данных в Sanity, отчёт по блоку
**обязан** открываться отдельным, визуально выделенным разделом — раньше описания
того, что сделано в коде:

```
## ⚠️ ЧТОБЫ УВИДЕТЬ РЕЗУЛЬТАТ, СНАЧАЛА СДЕЛАЙ В STUDIO:
1. Открыть <ссылка на Studio>
2. Открыть документ «...»
3. Заполнить поля: ... (перечислить конкретно, что обязательно, а что нет)
4. Нажать Publish
```

Если блок ничего не требует от заказчика (например, только код или уже
опубликованные данные) — так и написать явно: «Действий в Sanity не требуется,
можно смотреть сразу».

**Также по умолчанию держать Studio задеплоенной.** Хостинг-URL Studio
(`npm run sanity:deploy`) — это единственное место, где заказчик реально правит
контент; локальная `npm run sanity:dev` ему не нужна. После **любого** изменения
схемы (`sanity/schemas/*`, `sanity/structure.js`) — сразу прогонять
`npm run sanity:deploy`, не дожидаясь, пока попросят. Деплой не разрушает данные
и не требует подтверждения — можно запускать сразу же после правки схемы.

### 7. Анимационной библиотеки в проекте нет

`framer-motion` не установлен. Анимации делаем на CSS (`@keyframes` в `globals.css` +
Tailwind-классы) и на `IntersectionObserver` для появления при скролле.
Ставить новую зависимость — **только с явного согласия заказчика**.
Обязательно уважать `prefers-reduced-motion`.

### 8. Мобильная версия — обязательна для каждого блока, не бонус после

**Правило появилось 2026-08-11**, после того как заказчик посмотрел мобильную HERO
и она не устроила (кнопки вертикальные и слишком тяжёлые, фото коллажа вообще не
показаны — `hidden` до `lg`). Причина — в Figma-макете мобильной версии нет
(см. `backlog.md`, п.4), и до этого момента мобильная раскладка молчаливо
доделывалась «как получится», без отдельного показа заказчику.

Начиная с этого момента, для **каждого** блока мобильная версия — часть сдачи блока,
не отложенная задача:

- Верстаем и её тоже, не только десктоп по Figma. Ориентиров из макета для неё нет —
  решения по композиции принимает агент, ориентируясь на десктопную версию блока
  (те же фото, тот же текст, тот же смысл раскладки), но не обязан копировать
  десктопную структуру пиксель-в-пиксель.
- В отчёте по блоку — **отдельный раздел с мобильной версией**: что сделано,
  скриншот/описание раскладки, чем она отличается от десктопной и почему.
- Заказчик утверждает мобильную версию блока отдельно от десктопной — «принято» по
  блоку означает принято и то, и другое. Если заказчик просит правки только по
  мобильной части — это не блокирует переход к следующему блоку по десктопу, но
  сам блок в `blocks.md` остаётся не-✅, пока не закрыты оба.
- Проверять на ширинах ≤767px (мобильный) отдельно от диапазона 1024–1919
  (планшет/ноутбук, см. баг из `backlog.md` п.1.5) — это разные раскладки.

### 9. `vw`-размеры — только через `clamp()`, никогда голым числом

**Правило появилось 2026-08-11**, после того как заказчик поймал на широком экране
одновременно два случая одного и того же бага в разных блоках (GUESTS, потом ещё
раз HERO и ABOUT) — заголовки и отступы, завязанные на `vw` (резиновое
масштабирование 1:1 с шириной макета), росли **без верхней границы** и на широких
мониторах (2500px+) становились абсурдно большими и/или рвали раскладку, если
колонка при этом капалась `Container`-ом (`max-w-screen-xl`=1280px), а шрифт — нет.

Начиная с этого момента, **любой** `lg:text-[Xvw]` / `lg:mt-[Xvw]` / `lg:gap-[Xvw]` и
т.п. — пишем сразу как `lg:text-[clamp(MIN,Xvw,MAX)]`, не голым `Xvw`:

- `MAX` — значение той же `X` при 1920px (макет): `X% × 19.2`, округлить до px.
- `MIN` — значение из предыдущего (не-`lg`) тира (`sm:`/`md:`), чтобы не было
  провала размера сразу после пересечения `lg`-брейкпоинта (Tailwind `lg` = 1024px,
  это раньше, чем большинство контейнеров реально упираются в свой максимум).

Не важно, обёрнут ли блок в `Container` — полноширинные блоки (HERO, ABOUT) страдают
от той же проблемы, просто по другой причине (у них самих нет верхней границы
ширины). Проверять каждый новый блок на ширине **2500–3000px** отдельно от
обычных 1920/1440 — баг на них не виден.

⚠️ **Если в блоке рядом есть и `vw`-текст, и `%`-от-контейнера позиционирование**
(фото-коллажи с абсолютными процентными координатами, как в HERO/ABOUT/GUESTS) —
клампить нужно **и то, и другое**, иначе после клампа только текста они разойдутся
по вертикали на широком экране (текст «замрёт», фото — нет, продолжат расти вместе
с контейнером). Для этого ограничивать **сам контейнер**: `lg:max-w-[<ширина макета
в px>]` на элементе с `aspect-ratio` — тогда все проценты внутри него тоже
останавливаются в одной и той же точке, что и `clamp()`-текст. См. `backlog.md` п.0,
раздел про HERO/ABOUT — там разобран этот случай подробно.

---

## Как проверять результат

Главная страница `/` **закрыта публично** — `middleware.ts` редиректит на `/tours/kas`.
Смотреть главную только по адресу:

```
http://localhost:3000/admin/
```

Перед сдачей каждого блока прогонять:

```bash
npx tsc --noEmit
```

Тестов в проекте нет — проверка визуальная + типы + сборка.

---

## Рабочий процесс (согласован с заказчиком)

Работаем **по одному блоку за раз**, не пачками:

1. Агент снимает блок из Figma (`get_screenshot` + `get_design_context`).
2. Сверяет с [`blocks.md`](blocks.md): что за данные, что уже есть в Sanity, что новое.
3. Реализует **этот блок целиком — десктоп и мобильную версию** (см. правило 8:
   мобильная не Figma-референс, решения по композиции на усмотрение агента).
4. Прогоняет `npx tsc --noEmit`, смотрит через `/admin/` на десктопных и мобильных
   ширинах.
5. Даёт заказчику **краткий отчёт** с двумя разделами — десктоп и мобильная версия:
   что сделано, что взято из Sanity, что нужно заполнить в Studio, какие остались
   вопросы.
6. Заказчик проверяет обе версии → даёт добро → переходим к следующему блоку.
7. Агент отмечает статус блока в [`blocks.md`](blocks.md) и записывает туда новые
   комментарии заказчика (по десктопу и по мобильной — отдельно, если различаются).

**Не забегать вперёд.** Не начинать следующий блок, пока предыдущий не принят.

---

## Куда писать состояние

Всё, что нужно помнить между сессиями, — **в файлы этой папки**, не в память агента:

- новые пожелания заказчика по блоку → в `blocks.md`, в строку блока;
- решения по схемам Sanity → в `sanity-plan.md`;
- изменения общих правил → сюда.
